[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "longdur_etth1"
version = "1.0.0"
description = "Long-Duration Forecasting of ETTh1"
readme = "README.md"
requires-python = ">=3.13.11"
dependencies = [
    "numpy",
    "matplotlib",
    "pandas",
    "torch",
    "scikit-learn",
    "ipykernel",
    "pmdarima"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["models*", "utils*"]
